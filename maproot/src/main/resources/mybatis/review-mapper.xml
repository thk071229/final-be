<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="review">

<select id="sequence" resultType="int">
	select review_seq.nextval from dual
</select>

<insert id="insert">
	insert into review(
		review_no, schedule_no, 
		review_writer_type, account_id,
		review_writer_nickname, review_content,
		review_wtime
	) values (
	#{reviewNo}, #{scheduleNo},
		#{reviewWriterType} , #{accountId},
		#{reviewWriterNickname}, #{reviewContent},
		#{reviewWtime}
	)
</insert>

<delete id="delete">
	delete from review where review_no=#{reviewNo}
</delete>

<update id="update">
	update review set 
	review_content=#{reviewContent}, review_etime = systimestamp
	where review_no = #{reviewNo}
</update>
</mapper>