<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="reviewScheduleLink">

<select id="select" resultType="ReviewScheduleLinkVO">
	select * from review_schedule_link 
	<where>
 		schedule_no = #{scheduleNo}
	</where>
</select>

</mapper>