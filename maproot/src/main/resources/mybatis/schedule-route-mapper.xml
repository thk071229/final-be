<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="scheduleRoute">

	<select id="sequence" resultType="long">
		select schedule_route_seq.nextval from dual
	</select>
	
	<insert id="insert">
		insert into schedule_route(
			schedule_route_no, schedule_no, schedule_route_key, schedule_route_start, schedule_route_end,
			schedule_route_time, schedule_route_distance, schedule_route_priority, schedule_route_geom
		) 
		values (
			#{scheduleRouteNo}, #{scheduleNo}, #{scheduleRouteKey}, #{scheduleRouteStart}, #{scheduleRouteEnd},
			#{scheduleRouteTime}, #{scheduleRouteDistance}, #{scheduleRoutePriority},
			SDO_GEOMETRY(
	            2002, 4326, NULL,
	            SDO_ELEM_INFO_ARRAY(1, 2, 1),
	            SDO_ORDINATE_ARRAY( ${ordinateString} ) 
	        )
		)
	</insert>
	
	<select id="detail" resultType="ScheduleRouteDto">
		select * from schedule_route where schedule_route_no = #{schedultRouteNo}
	</select>
	
</mapper>